server:
  extraArgs:
    - --insecure
  service:
    type: ClusterIP

# Ingress를 통한 ArgoCD 노출 설정
ingress:
  enabled: true
  ingressClassName: nginx
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
  hosts:
    - argocd.team1.kosops.com
  paths:
    - /
